<div class="main-header clearfix">
    <div class="page-title">
        <h3 class="no-margin"><%= @fpclass.name.capitalize %></h3>
        <span></span>
    </div><!-- /page-title -->
    <ul class="page-stats">
        
        
    </ul><!-- /page-stats -->
</div>


<div class="padding-md">
    <div class="panel panel-default table-responsive">
        <div class="panel-heading">
            Recent Bookings
        </div>
        <div class="panel-body">
            <!-- /.row -->
        </div>
        <table class="table table-striped table-responsive" id="responsiveTable">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Customer Name</th>
                    <th>Booked on</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
            <%unless @bookings.blank? %>
               <% @bookings.each do |booking| %>
                <tr>
                    <td><a href=""></a></td>
                    <td><%= booking.try(:customer).try(:user).try(:full_name) %></td>
                    <td><%= local_time(booking.created_at) %></td>
                    <td><span class="label label-success">Paid</span></td>
                </tr>
                <% end %>
             <% end %>
             <% if @bookings.blank? %>
                <p>No Bookings</p>
             <% end %>
            </tbody>
        </table>
        <div class="panel-footer clearfix">
            
        </div>
    </div>
</div>