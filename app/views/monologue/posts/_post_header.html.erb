<h4><%= link_to post.title, post.full_url %></h4>
<small class="text-muted">By <a href="#"><strong> <span data-monologue="user"><%= post.user.name %></span></strong></a> |  Post on <span data-monologue="time"><time datetime="<%= post.published_at %>"><%= I18n.localize(post.published_at.to_date, format: :long) %></time></span> <span data-monologue="disqus"><a href="<%= post.full_url + "#disqus_thread" %>"></a> </span></small>