<%
meta title: "Popular | FitnessPapa.com",
     description: "",
     keywords: %w(services popular gym swimming)
%>


<%= render :partial => 'layouts/header' %>

<div class="row banner">
 <div class="col-xs-12">
 	 <div class="item">
 	 	<img src="/assets/bg-5.jpg" alt="image" class="img-responsive">
 		 <div class="search-panel text-center">
         	<div class="row">
         		<div class="col-sm-12 col-md-6 col-md-offset-3">
		         	<div class="search-sec-main">
						<form accept-charset="UTF-8" action="/filters/list" class="services" id="center_search" method="get" onsubmit="return search_validator();">
						<div class="row">
							<div class="col-xs-3">
								<select id="q_centerinfo_categories_id_in" name="q[centerinfo_categories_id_in]" class="form-control"><option value="2">Gym</option>
									<option value="5">Swimming</option>
									<option value="1">Yoga</option>
								</select>
							</div>
							<div class="col-xs-7">
								<input id="cities" type="text" name="q[city_name_cont]" placeholder="Type Locality or Landmark" class="ui-autocomplete-input form-control" autocomplete="off">		
							</div>
							<div class="col-xs-2">
								<input type="submit" value="Search" class="form-control btn btn-primary">
							</div>
						</div>
					</form>			
		         	</div>
         		</div>
         	</div>
         </div>
 	 </div>
 </div>	
</div>


<div class="container">
	<div class="row">
		<div class="col-xs-12">
			<h2 class="text-center">Offers Zone</h2>
		</div>
	</div>
	<div class="row">
	<% unless @centers.nil? %>
	<% @centers.each do |center| %>
		<div class="col-xs-6">
			<div class="offers-box">
				<h2><a href="<%= center_category_services_path(center.friendly_id, params[:category_id]) %>"><%= center.name.camelize %></a></h2>
				<a href="<%= center_category_services_path(center.friendly_id, params[:category_id]) %>">
				<% unless center.try(:centerinfo).try(:image).nil? %>
				<%= image_tag attachment_url(center.centerinfo, :image, :fill, 503, 287), :class => 'img-responsive img-thumbnail' %>
				<%end %>
				</a>
				<p class="text-center"><img src="/assets/star.jpg" alt=""></p>
			</div>
		</div>
 	<%end%>
	<%end%>
	</div>
</div>
	
<%= render :partial => 'layouts/footer' %>

<script>
$(function() {
	var availableTags = <%= raw @city_names %>;
	$("#cities").autocomplete({
	  source: availableTags
	});
});
function search_validator(){
	if(document.getElementById('cities').value == ""){  
      alert("Please select city");  
	  return false;  
    }else{  
      return true;  
    }  
}ssddssssxxx
</script>

