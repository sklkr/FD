<div class="footer-quick-link" style="bottom:0px">
  <div class="quick-link">
	<p class="copyright">&copy; 2015.  StunApp Technologies & Services Pvt Ltd.      All Rights Reserved.</p>
  </div>
</div>

<% [:notice, :error, :success, :warning].each do |n| %>
<%if flash[n] %>
    <script>var n = noty({text: '<%= flash[n] %>',type: '<%= n %>', layout: 'bottom',animation: {
    open: 'animated flipInX', // Animate.css class names
    close: 'animated flipOutX', // Animate.css class names
    theme: 'relax',
    dismissQueue: true,
    speed: 500
    }});  </script>
<%end %>
<%end%>
<%unless flash['model_errors'].nil? %>
<%flash['model_errors'].each do |error| %>
    <%error[1].each do |msg| %>
        <script>var n = noty({text: '<%= error[0] %> <%= msg %>',type: 'error', theme: 'relax',dismissQueue: true, layout: 'bottom',animation: {
        open: 'animated flipInX', // Animate.css class names
        close: 'animated flipOutX', // Animate.css class names
        easing: 'swing', // unavailable - no need
        speed: 500 // unavailable - no need
        }});  </script>
    <%end%>
<%end%>
<%end%>